import { Component } from "react";

import 'bootstrap/dist/css/bootstrap.min.css';

// entry point to application in Next.js
export default ({ Component, pageProps }) => <Component {...pageProps} />;
